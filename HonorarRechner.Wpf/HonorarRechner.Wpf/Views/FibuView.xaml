<UserControl x:Class="HonorarRechner.Wpf.Views.FibuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1300">

    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="20" ShadowDepth="2" Opacity="0.25" Direction="270"/>

        <Style x:Key="RowLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,15"/>
        </Style>

        <Style x:Key="RowValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,15"/>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" CornerRadius="10" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid>
        <Border Background="#0077BE" CornerRadius="40" Margin="30" Padding="50" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="100" Height="100" CornerRadius="20" Background="White" Margin="0,0,30,0">
                        <Image Source="/SFS Logo.png" Stretch="Uniform"/>
                    </Border>

                    <TextBlock Grid.Column="1" Text="FiBu - Finanzbuchhaltung" 
                               FontSize="36" FontWeight="Bold" Foreground="White" 
                               TextDecorations="Underline" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>

                <Grid Grid.Row="1" VerticalAlignment="Center" MaxWidth="1000" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Satz / Umsatz" Foreground="#DDFFFFFF" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Monatlich" Foreground="#DDFFFFFF" FontSize="16" HorizontalAlignment="Right" Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Laufende Buchführung" Style="{StaticResource RowLabel}" FontWeight="Bold"/>
                        <TextBlock Text="nach Gegenstandswerttabelle" Foreground="#CCFFFFFF" FontSize="14" Margin="0,-10,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Umsatz:" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding UmsatzText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}" TextAlignment="Right"/>
                        <TextBlock Text="{Binding SatzText, StringFormat='Satz: {0}'}" Foreground="#CCFFFFFF" FontSize="14" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding LaufendeFibuMonatlich}" Style="{StaticResource RowValue}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="IT Pauschale" Style="{StaticResource RowLabel}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="Pauschale" Foreground="White" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding ItPauschaleText}" Style="{StaticResource RowValue}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Auslagenpauschale" Style="{StaticResource RowLabel}"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding AuslagenPauschaleProzentText}" Foreground="White" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="5" Grid.Column="2" Text="{Binding AuslagenPauschaleWert}" Style="{StaticResource RowValue}"/>

                    <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3" BorderBrush="#80FFFFFF" BorderThickness="0,1,0,0" Padding="0,20,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,5">
                                    <TextBlock Text="monatlich: " Foreground="#DDFFFFFF" FontSize="18" VerticalAlignment="Bottom" Margin="0,0,10,2"/>
                                    <TextBlock Text="{Binding ZwischenSummeMonat}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="jährlich: " Foreground="#DDFFFFFF" FontSize="18" VerticalAlignment="Bottom" Margin="0,0,10,2"/>
                                    <TextBlock Text="{Binding ZwischenSummeJahr}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="min. 208,00 €" Foreground="#AAFFFFFF" FontSize="14" HorizontalAlignment="Right" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Button Content="Zurück" Command="{Binding ZurueckCommand}" 
                            Width="160" Height="50" HorizontalAlignment="Left"
                            FontSize="18" FontWeight="SemiBold"
                            Background="#20FFFFFF" Foreground="White" BorderBrush="White" BorderThickness="1.5" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="25" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#40FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</UserControl>