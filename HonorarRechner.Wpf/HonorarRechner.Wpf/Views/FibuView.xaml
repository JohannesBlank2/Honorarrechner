<UserControl x:Class="HonorarRechner.Wpf.Views.FibuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="RowLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
        <Style x:Key="RowValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" CornerRadius="10" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" MaxWidth="1000" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Satz / Umsatz" Foreground="#DDFFFFFF" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="0" Grid.Column="2" Text="Monatlich" Foreground="#DDFFFFFF" FontSize="16" HorizontalAlignment="Right" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,20">
            <TextBlock Text="Laufende Buchführung" Style="{StaticResource RowLabel}" FontWeight="Bold" Margin="0"/>
            <TextBlock Text="nach Gegenstandswerttabelle" Foreground="#CCFFFFFF" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20">
            <TextBlock Text="Umsatz:" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,4"/>
            <TextBox Text="{Binding UmsatzText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}" TextAlignment="Right" Margin="10,0,0,0"/>
            <TextBlock Text="{Binding SatzText, StringFormat='Satz: {0}'}" Foreground="#CCFFFFFF" FontSize="14" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding LaufendeFibuMonatlich}" Style="{StaticResource RowValue}" Margin="0,20"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="IT Pauschale" Style="{StaticResource RowLabel}" Margin="0,10"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="Pauschale" Foreground="White" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10"/>
        <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding ItPauschaleText}" Style="{StaticResource RowValue}" Margin="0,10"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Auslagenpauschale" Style="{StaticResource RowLabel}" Margin="0,10"/>
        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding AuslagenPauschaleProzentText}" Foreground="White" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10"/>
        <TextBlock Grid.Row="5" Grid.Column="2" Text="{Binding AuslagenPauschaleWert}" Style="{StaticResource RowValue}" Margin="0,10"/>

        <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3" BorderBrush="#80FFFFFF" BorderThickness="0,1,0,0" Padding="0,20,0,0"/>
    </Grid>
</UserControl>