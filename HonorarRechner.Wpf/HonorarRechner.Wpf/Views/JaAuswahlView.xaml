<UserControl x:Class="HonorarRechner.Wpf.Views.JaAuswahlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:HonorarRechner.Wpf.Converters"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="1000">

    <UserControl.Resources>
        <conv:BoolToOpacityConverter x:Key="BoolToOpacity" />
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="BigOptionRadio" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="root" Width="260" Height="160" CornerRadius="25" 
                                Background="#10FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="2" Margin="15">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="{TemplateBinding Content}" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                <TextBlock Text="{TemplateBinding Tag}" FontSize="14" Foreground="#CCFFFFFF" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <Grid Width="24" Height="24" Margin="0,15,0,0">
                                    <Ellipse Stroke="White" StrokeThickness="2"/>
                                    <Ellipse Width="12" Height="12" Fill="#26D1DE" 
                                             Opacity="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToOpacity}}"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#0069AA"/>
                                <Setter TargetName="root" Property="BorderBrush" Value="#FFFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Grid Width="22" Height="22" Margin="0,0,8,0">
                                <Ellipse Stroke="White" StrokeThickness="2"/>
                                <Ellipse x:Name="dot" Width="10" Height="10" Fill="#26D1DE" Opacity="0"/>
                            </Grid>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="dot" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#CCFFFFFF"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" Foreground="{TemplateBinding Foreground}"/>
                            <Path Data="M 0,0 L 4,4 L 0,8" Stroke="{TemplateBinding Foreground}" StrokeThickness="2" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#26D1DE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
            <RadioButton GroupName="MainType" Content="EÜR" Tag="(Einnahmen Überschuss Rechner)" 
                         IsChecked="{Binding IsEuerSelected}" Style="{StaticResource BigOptionRadio}"/>

            <Button Content="zum EÜR" 
                    Command="{Binding OpenEuerCommand}"
                    Visibility="{Binding IsEuerSelected, Converter={StaticResource BoolToVis}}"
                    Style="{StaticResource LinkButton}"/>
        </StackPanel>

        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
            <RadioButton GroupName="MainType" Content="Bilanz" Tag="(Bilanz Rechner)" 
                         IsChecked="{Binding IsBilanzSelected}" Style="{StaticResource BigOptionRadio}"/>

            <Button Content="zur Bilanz" 
                    Command="{Binding OpenBilanzCommand}"
                    Visibility="{Binding IsBilanzSelected, Converter={StaticResource BoolToVis}}"
                    Style="{StaticResource LinkButton}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0"
                        Visibility="{Binding IsBilanzSelected, Converter={StaticResource BoolToVis}}">

                <RadioButton GroupName="SubType" Content="Einzelunternehmen" IsChecked="{Binding IsEinzelunternehmen}"
                             Style="{StaticResource ModernRadioButton}" Margin="0,0,20,0"/>

                <RadioButton GroupName="SubType" Content="Gesellschaft" IsChecked="{Binding IsGesellschaft}"
                             Style="{StaticResource ModernRadioButton}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>