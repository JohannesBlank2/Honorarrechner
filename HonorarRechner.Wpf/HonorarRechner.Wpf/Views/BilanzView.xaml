<UserControl x:Class="HonorarRechner.Wpf.Views.BilanzView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="RowLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,8"/>
        </Style>
        <Style x:Key="RowValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,8"/>
        </Style>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" CornerRadius="6" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SatzLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#DDFFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Aufstellung des Jahresabschlusses" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding AufstellungSatz}" Style="{StaticResource SatzLabel}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding AufstellungInput, UpdateSourceTrigger=LostFocus}"/>
                <TextBlock Grid.Column="2" Text="{Binding AufstellungText}" Style="{StaticResource RowValue}"/>
            </Grid>

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Erstellung des Antrags" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding AntragSatz}" Style="{StaticResource SatzLabel}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding AntragInput, UpdateSourceTrigger=LostFocus}"/>
                <TextBlock Grid.Column="2" Text="{Binding AntragText}" Style="{StaticResource RowValue}"/>
            </Grid>

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Entwicklung einer Steuerbilanz" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding SteuerbilanzSatz}" Style="{StaticResource SatzLabel}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding SteuerbilanzInput, UpdateSourceTrigger=LostFocus}"/>
                <TextBlock Grid.Column="2" Text="{Binding SteuerbilanzText}" Style="{StaticResource RowValue}"/>
            </Grid>

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Körperschaftssteuererklärung" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding KoerperschaftSatz}" Style="{StaticResource SatzLabel}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding KoerperschaftInput, UpdateSourceTrigger=LostFocus}"/>
                <TextBlock Grid.Column="2" Text="{Binding KoerperschaftText}" Style="{StaticResource RowValue}"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Column="2">

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Umsatzsteuererklärung für das KJ" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding UstSatz}" Style="{StaticResource SatzLabel}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding UstInput, UpdateSourceTrigger=LostFocus}"/>
                <TextBlock Grid.Column="2" Text="{Binding UstText}" Style="{StaticResource RowValue}"/>
            </Grid>

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Erklärung zur Gewerbesteuer" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding GewerbeSatz}" Style="{StaticResource SatzLabel}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding GewerbeInput, UpdateSourceTrigger=LostFocus}"/>
                <TextBlock Grid.Column="2" Text="{Binding GewerbeText}" Style="{StaticResource RowValue}"/>
            </Grid>

            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Bescheide" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="Anzahl" Style="{StaticResource SatzLabel}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding BescheidAnzahl, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding BescheidText}" Style="{StaticResource RowValue}"/>
            </Grid>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="E-Bilanz Pauschale" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding EBilanzText}" FontWeight="Bold" Foreground="White" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="Offenlegung Pauschale" Style="{StaticResource RowLabel}" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding OffenlegungText}" FontWeight="Bold" Foreground="White" FontSize="16" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>

            <Border Margin="0,20,0,0" BorderBrush="White" BorderThickness="0,1,0,0" Padding="0,10">
                <Grid>
                    <TextBlock Text="Zwischensumme Bilanz (Jahr):" Style="{StaticResource RowLabel}"/>
                    <TextBlock Text="{Binding ZwischenSummeJahr}" Style="{StaticResource RowValue}"/>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>