<UserControl x:Class="HonorarRechner.Wpf.Views.EuerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1300">

    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="20" ShadowDepth="2" Opacity="0.25" Direction="270"/>

        <Style x:Key="RowLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
        <Style x:Key="RowValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border Background="#0077BE" CornerRadius="40" Margin="30" Padding="50" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="100" Height="100" CornerRadius="20" Background="White" Margin="0,0,20,0">
                        <Image Source="/SFS Logo.png" Stretch="Uniform"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="EÜR" FontSize="36" FontWeight="Bold" Foreground="White" TextDecorations="Underline" HorizontalAlignment="Center"/>
                        <TextBlock Text="(Einnahmen Überschuss Rechner)" FontSize="20" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" VerticalAlignment="Center" MaxWidth="1000">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Satz" Foreground="#DDFFFFFF" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Jährlich" Foreground="#DDFFFFFF" HorizontalAlignment="Right" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Betriebs Einnahmen-Ausgaben" Style="{StaticResource RowLabel}"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center">
                        <TextBox Style="{StaticResource InputBox}" Text="0,00 €" IsReadOnly="True" Background="#E0E0E0" TextAlignment="Right"/>
                        <TextBlock Text="{Binding BeaSatz}" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding BeaText}" Style="{StaticResource RowValue}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Gewerbesteuer" Style="{StaticResource RowLabel}"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center">
                        <TextBox Style="{StaticResource InputBox}" Text="0,00 €" IsReadOnly="True" Background="#E0E0E0" TextAlignment="Right"/>
                        <TextBlock Text="{Binding GewerbeSatz}" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding GewerbeText}" Style="{StaticResource RowValue}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Überschuss der Betriebseinnahmen" Style="{StaticResource RowLabel}"/>
                    <StackPanel Grid.Row="3" Grid.Column="1" HorizontalAlignment="Center">
                        <TextBox Style="{StaticResource InputBox}" Text="0,00 €" IsReadOnly="True" Background="#E0E0E0" TextAlignment="Right"/>
                        <TextBlock Text="{Binding UedbSatz}" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding UedbText}" Style="{StaticResource RowValue}"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Umsatzsteuererklärung" Style="{StaticResource RowLabel}"/>
                    <StackPanel Grid.Row="4" Grid.Column="1" HorizontalAlignment="Center">
                        <TextBox Style="{StaticResource InputBox}" Text="0,00 €" IsReadOnly="True" Background="#E0E0E0" TextAlignment="Right"/>
                        <TextBlock Text="{Binding UstSatz}" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding UstText}" Style="{StaticResource RowValue}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Pauschale für Abschlussarbeiten" Style="{StaticResource RowLabel}"/>
                    <StackPanel Grid.Row="5" Grid.Column="1" HorizontalAlignment="Center">
                        <TextBox Style="{StaticResource InputBox}" Text="{Binding BescheidAnzahl}" TextAlignment="Center"/>
                        <TextBlock Text="Anzahl an Bescheiden" FontSize="10" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Grid.Row="5" Grid.Column="2" Text="{Binding PauschaleText}" Style="{StaticResource RowValue}"/>

                    <StackPanel Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Right">
                        <TextBlock Text="min. 110,00 €" Foreground="#AAFFFFFF" HorizontalAlignment="Right" FontSize="12"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="monatlich: " Foreground="#DDFFFFFF" FontSize="16" VerticalAlignment="Bottom" Margin="0,0,10,2"/>
                            <TextBlock Text="{Binding ZwischenSummeMonat}" Foreground="White" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="jährlich: " Foreground="#DDFFFFFF" FontSize="16" VerticalAlignment="Bottom" Margin="0,0,10,2"/>
                            <TextBlock Text="{Binding ZwischenSummeJahr}" Foreground="White" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Button Content="Zurück" Command="{Binding ZurueckCommand}" 
                            Width="150" Height="45" FontSize="16" HorizontalAlignment="Left"
                            Background="#20FFFFFF" Foreground="White">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="22" BorderBrush="White" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>