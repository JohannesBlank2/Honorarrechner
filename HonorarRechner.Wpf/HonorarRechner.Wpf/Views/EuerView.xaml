<UserControl x:Class="HonorarRechner.Wpf.Views.EuerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1300">

    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="25" ShadowDepth="4" Opacity="0.35" Direction="270"/>

        <Style x:Key="RowLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style x:Key="RowValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="MinInfoText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAFFFFFF"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>

        <Style x:Key="ValueInputBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SatzBoxBorder" TargetType="Border">
            <Setter Property="Background" Value="#25FFFFFF"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SatzBoxText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="RowCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Grid Width="24" Height="24" Margin="0,0,10,0">
                                <Border x:Name="box" CornerRadius="6" Background="#20FFFFFF" BorderBrush="#80FFFFFF" BorderThickness="2">
                                    <Border.RenderTransform>
                                        <ScaleTransform x:Name="boxScale" ScaleX="1" ScaleY="1" CenterX="12" CenterY="12"/>
                                    </Border.RenderTransform>
                                </Border>
                                <Viewbox Margin="4">
                                    <Path x:Name="tick" Data="M 0,5 L 4,9 L 10,0" Stroke="White" StrokeThickness="2" 
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0"/>
                                </Viewbox>
                            </Grid>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="Background" Value="#26D1DE"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="Transparent"/>
                                <Setter TargetName="tick" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="30,50,30,30" To="30,30,30,30" Duration="0:0:0.4" DecelerationRatio="0.9"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid>
        <Border Background="#0077BE" CornerRadius="40" Margin="20" Padding="40" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="100" Height="100" CornerRadius="20" Background="White" Margin="0,0,20,0">
                        <Image Source="/SFS Logo.png" Stretch="Uniform"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="EÜR" FontSize="36" FontWeight="Bold" Foreground="White" TextDecorations="Underline" HorizontalAlignment="Center"/>
                        <TextBlock Text="(Einnahmen Überschuss Rechner)" FontSize="20" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" VerticalAlignment="Center" MaxWidth="1100" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1.5*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Gegenstandswert" Foreground="#DDFFFFFF" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Satz" Foreground="#DDFFFFFF" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Jährlich" Foreground="#DDFFFFFF" HorizontalAlignment="Right" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,0,20" Text="Betriebs Einnahmen-Ausgaben" Style="{StaticResource RowLabel}"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">
                        <TextBox Text="{Binding BeaInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ValueInputBox}"/>
                        <TextBlock Text="min. 25,00 €" Style="{StaticResource MinInfoText}"/>
                    </StackPanel>
                    <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource SatzBoxBorder}">
                        <TextBlock Text="{Binding BeaSatz}" Style="{StaticResource SatzBoxText}"/>
                    </Border>
                    <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding BeaResultText}" Style="{StaticResource RowValue}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0"  Text="Gewerbesteuer" Style="{StaticResource RowLabel}" Margin="0,15,10,20"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Margin="0,15,0,0">
                        <TextBox Text="{Binding GewerbeInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ValueInputBox}"/>
                        <TextBlock Text="min. 10,00 €" Style="{StaticResource MinInfoText}"/>
                    </StackPanel>
                    <Border Grid.Row="2" Grid.Column="2" Style="{StaticResource SatzBoxBorder}" Margin="0,15,0,20">
                        <TextBlock Text="{Binding GewerbeSatz}" Style="{StaticResource SatzBoxText}"/>
                    </Border>
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="{Binding GewerbeResultText}" Style="{StaticResource RowValue}" Margin="0,15,0,0"/>

                    <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15,10,20">
                        <CheckBox Style="{StaticResource RowCheckBox}" IsChecked="{Binding IsUeberschussSelected}" Content="Überschuss der Betriebseinnahmen"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" Margin="0,15,0,0">
                        <TextBox Text="{Binding UedbInput, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsUeberschussSelected}">
                            <TextBox.Style>
                                <Style TargetType="TextBox" BasedOn="{StaticResource ValueInputBox}">
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.5"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        <TextBlock Text="min. 20,00 €" Style="{StaticResource MinInfoText}"/>
                    </StackPanel>

                    <Border Grid.Row="3" Grid.Column="2" Margin="0,15,0,20">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource SatzBoxBorder}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsUeberschussSelected}" Value="False">
                                        <Setter Property="Opacity" Value="0.3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding UedbSatz}" Style="{StaticResource SatzBoxText}"/>
                    </Border>

                    <TextBlock Grid.Row="3" Grid.Column="3" Text="{Binding UedbResultText}" Margin="0,15,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource RowValue}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsUeberschussSelected}" Value="False">
                                        <Setter Property="Opacity" Value="0.3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Umsatzsteuererklärung" Style="{StaticResource RowLabel}" Margin="0,15,10,20"/>
                    <StackPanel Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" Margin="0,15,0,0">
                        <TextBox Text="{Binding UstInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ValueInputBox}"/>
                        <TextBlock Text="min. 15,00 €" Style="{StaticResource MinInfoText}"/>
                    </StackPanel>
                    <Border Grid.Row="4" Grid.Column="2" Style="{StaticResource SatzBoxBorder}" Margin="0,15,0,20">
                        <TextBlock Text="{Binding UstSatz}" Style="{StaticResource SatzBoxText}"/>
                    </Border>
                    <TextBlock Grid.Row="4" Grid.Column="3" Text="{Binding UstResultText}" Style="{StaticResource RowValue}" Margin="0,15,0,0"/>

                    <StackPanel Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,15,10,20">
                        <TextBlock Text="Pauschale für Abschlussarbeiten" Style="{StaticResource RowLabel}"/>
                        <TextBlock Text="(Prüfung von Bescheiden)" Foreground="#CCFFFFFF" FontSize="12" Margin="0,-3,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Row="5" Grid.Column="1" VerticalAlignment="Center" Margin="0,15,0,0">
                        <TextBox Text="{Binding PauschaleInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ValueInputBox}" Width="80"/>
                        <TextBlock Text="Anzahl" Style="{StaticResource MinInfoText}"/>
                    </StackPanel>
                    <TextBlock Grid.Row="5" Grid.Column="3" Text="{Binding PauschaleResultText}" Style="{StaticResource RowValue}" Margin="0,15,0,0"/>

                    <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="4" BorderBrush="#80FFFFFF" BorderThickness="0,1,0,0" Padding="0,20,0,0">
                        <StackPanel HorizontalAlignment="Right">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,5">
                                <TextBlock Text="monatlich: " Foreground="#DDFFFFFF" FontSize="18" VerticalAlignment="Bottom" Margin="0,0,10,2"/>
                                <TextBlock Text="{Binding MonatsAnteilText}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="jährlich: " Foreground="#DDFFFFFF" FontSize="18" VerticalAlignment="Bottom" Margin="0,0,10,2"/>
                                <TextBlock Text="{Binding JahresGesamtText}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Button Content="Zurück" Command="{Binding ZurueckCommand}" 
                            Width="150" Height="45" FontSize="16" HorizontalAlignment="Left"
                            Background="#20FFFFFF" Foreground="White">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="22" BorderBrush="White" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#40FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>