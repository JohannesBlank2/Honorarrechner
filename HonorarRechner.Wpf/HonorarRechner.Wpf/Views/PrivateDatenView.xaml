<UserControl x:Class="HonorarRechner.Wpf.Views.PrivateDatenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900">

    <UserControl.Resources>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border CornerRadius="40" Margin="30" Padding="50">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding ViewTitle}" FontSize="32" FontWeight="Bold" Foreground="White" 
                           TextDecorations="Underline" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <StackPanel Grid.Row="1" MaxWidth="600" HorizontalAlignment="Center">

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Nachname, Vorname:" Style="{StaticResource LabelText}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Nachname, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}" Width="160" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding Vorname, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}" Width="160"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Summe positive Einkünfte:" Style="{StaticResource LabelText}"/>
                        <TextBox Text="{Binding SummeEinkuenfteInput, UpdateSourceTrigger=LostFocus}" Style="{StaticResource InputBox}" Grid.Column="1"/>
                    </Grid>

                    <CheckBox Content="Zusammenveranlagung (Ehegatten)" 
                              IsChecked="{Binding IstZusammenVeranlagung}"
                              Foreground="White" FontSize="16" Margin="0,10"/>

                    <TextBlock Text="Hinweis: Dieser Wert ist die Basis für die Einkommensteuererklärung." 
                               Foreground="#CCFFFFFF" FontStyle="Italic" Margin="0,20,0,0"/>
                </StackPanel>

                <Grid Grid.Row="2" Margin="0,40,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>