<UserControl x:Class="HonorarRechner.Wpf.Views.LeistungenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:HonorarRechner.Wpf.Converters"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="500">

    <UserControl.Resources>
        <conv:BoolToOpacityConverter x:Key="BoolToOpacity" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,40,0"/>
        </Style>

        <Style x:Key="ColumnHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#DDFFFFFF"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="TextDecorations" Value="Underline"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,40,15"/>
        </Style>

        <Style x:Key="BouncyCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="20,0,10,0"/>
        </Style>

        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#CCFFFFFF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="15,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" Foreground="{TemplateBinding Foreground}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#26D1DE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" MaxWidth="1100" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Monatlich" Style="{StaticResource ColumnHeader}"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="Jährlich" Style="{StaticResource ColumnHeader}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding FiBuMonatlichFormatted}" Style="{StaticResource ValueText}"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding FiBuJaehrlichFormatted}" Style="{StaticResource ValueText}"/>
        <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15">
            <CheckBox Content="FiBu" IsChecked="{Binding FiBu}" Style="{StaticResource BouncyCheckBox}"/>
            <Button Content="zur FiBu" Command="{Binding NavigateToFibuCommand}" Visibility="{Binding FiBu, Converter={StaticResource BoolToVis}}" Style="{StaticResource LinkButton}"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding JAMonatlichFormatted}" Style="{StaticResource ValueText}"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding JAJaehrlichFormatted}" Style="{StaticResource ValueText}"/>
        <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15">
            <CheckBox Content="JA" IsChecked="{Binding JA}" Style="{StaticResource BouncyCheckBox}"/>
            <Button Content="zum JA" Command="{Binding NavigateToJaCommand}" Visibility="{Binding JA, Converter={StaticResource BoolToVis}}" Style="{StaticResource LinkButton}"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="{Binding LohnMonatlichFormatted}" Style="{StaticResource ValueText}"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding LohnJaehrlichFormatted}" Style="{StaticResource ValueText}"/>
        <StackPanel Grid.Row="3" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15">
            <CheckBox Content="Lohn" IsChecked="{Binding Lohn}" Style="{StaticResource BouncyCheckBox}"/>
            <Button Content="zum Lohn" Command="{Binding NavigateToLohnCommand}" Visibility="{Binding Lohn, Converter={StaticResource BoolToVis}}" Style="{StaticResource LinkButton}"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="{Binding SelbstbucherMonatlichFormatted}" Style="{StaticResource ValueText}"/>
        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelbstbucherJaehrlichFormatted}" Style="{StaticResource ValueText}"/>
        <StackPanel Grid.Row="4" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15">
            <CheckBox Content="Selbstbucher (+20%)" IsChecked="{Binding Selbstbucher}" Style="{StaticResource BouncyCheckBox}"/>
        </StackPanel>
    </Grid>
</UserControl>