<Window x:Class="HonorarRechner.Wpf.Views.PrivatLeistungAuswahlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Leistung auswaehlen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Width="900"
        Height="560">

    <Window.Resources>
        <DropShadowEffect x:Key="DialogShadow" BlurRadius="25" ShadowDepth="6" Opacity="0.35" Direction="270"/>

        <Style x:Key="DialogTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#003B5C"/>
        </Style>

        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#003B5C"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C9DDED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root" Background="{TemplateBinding Background}" CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#F1F7FB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#E2EEF7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="root" Property="Background" Value="#F2F6F9"/>
                                <Setter TargetName="root" Property="BorderBrush" Value="#D0DEE9"/>
                                <Setter Property="Foreground" Value="#8AA4B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogAccentButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#0077BE"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1689D3"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0F6FAE"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BFD5E7"/>
                    <Setter Property="Foreground" Value="#5C7C94"/>
                    <Setter Property="BorderBrush" Value="#A8C1D6"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#80000000">
        <Border Background="#F7FBFF"
                CornerRadius="18"
                Padding="22"
                Width="820"
                Height="500"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Effect="{StaticResource DialogShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Leistung auswaehlen" Style="{StaticResource DialogTitle}"/>
                    <TextBlock Text="Bitte eine Leistung markieren und hinzufuegen."
                               FontSize="13" Foreground="#2E5A78" Margin="0,6,0,0"/>
                </StackPanel>

                <ListView x:Name="OptionenList" Grid.Row="1" Margin="0,16,0,16"
                          ItemsSource="{Binding Optionen}"
                          SelectedItem="{Binding SelectedOption}"
                          Background="Transparent"
                          BorderThickness="0"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          MouseDoubleClick="OptionenList_MouseDoubleClick">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="Card" Background="White" CornerRadius="12" Padding="14"
                                    BorderBrush="#D6E6F2" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" FontSize="15"
                                               Foreground="#003B5C" TextWrapping="Wrap"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Preis, StringFormat={}{0:C}}" FontSize="15"
                                               FontWeight="SemiBold" Foreground="#003B5C"
                                               Margin="20,0,0,0" VerticalAlignment="Top"/>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsMouseOver}"
                                             Value="True">
                                    <Setter TargetName="Card" Property="Background" Value="#F4FAFF"/>
                                    <Setter TargetName="Card" Property="BorderBrush" Value="#9BC7EA"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}"
                                             Value="True">
                                    <Setter TargetName="Card" Property="Background" Value="#E8F3FF"/>
                                    <Setter TargetName="Card" Property="BorderBrush" Value="#0077BE"/>
                                    <Setter TargetName="Card" Property="BorderThickness" Value="2"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Abbrechen" Style="{StaticResource DialogButton}"
                            Click="Cancel_Click" IsCancel="True" Margin="0,0,10,0"/>
                    <Button Content="Hinzufuegen" Click="Add_Click" IsDefault="True">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource DialogAccentButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedItem, ElementName=OptionenList}" Value="{x:Null}">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
