using System;
using System.Windows.Input;

namespace HonorarRechner.Wpf.ViewModels
{
    /// <summary>
    /// Eine Standard-Implementierung für ICommand, damit Buttons Methoden im ViewModel aufrufen können.
    /// </summary>
    public class RelayCommand : ICommand
    {
        private readonly Action<object?> _execute;
        private readonly Predicate<object?>? _canExecute;

        public RelayCommand(Action<object?> execute, Predicate<object?>? canExecute = null)
        {
            _execute = execute ?? throw new ArgumentNullException(nameof(execute));
            _canExecute = canExecute;
        }

        public bool CanExecute(object? parameter)
        {
            return _canExecute == null || _canExecute(parameter);
        }

        public void Execute(object? parameter)
        {
            _execute(parameter);
        }

        // Sorgt dafür, dass die UI (Buttons) automatisch aktualisiert wird, wenn sich Bedingungen ändern
        public event EventHandler? CanExecuteChanged
        {
            add { CommandManager.RequerySuggested += value; }
            remove { CommandManager.RequerySuggested -= value; }
        }
    }
}